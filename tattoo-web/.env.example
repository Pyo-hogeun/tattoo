VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://localhost:4000
# Optional: only needed when the API returns relative image paths from a different origin.
# VITE_IMAGE_BASE_URL=https://cdn.example.com
